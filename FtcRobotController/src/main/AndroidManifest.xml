<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.qualcomm.ftcrobotcontroller">

  <!-- The Robot Controller app needs permissions for communication, camera, and storage -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
  <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />

  <application
      android:allowBackup="true"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">

    <!-- This is the primary fix: android:exported="true" is now explicitly set
         for the main activity that uses the LAUNCHER intent filter.
         This is required for all apps targeting Android 12 (API 31) and higher. -->
    <activity
        android:name=".FtcRobotControllerActivity"
        android:configChanges="orientation|screenSize"
        android:exported="true"
        android:label="@string/app_name"
        android:launchMode="singleTask"
        tools:targetApi="31">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- Other necessary application components would be listed here, e.g., for
         scanning devices or providing data, and they would also need
         explicit 'android:exported' settings depending on their intent filters. -->

  </application>
</manifest>
